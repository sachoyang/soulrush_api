/* ============================================================
 *  config.js — 관리자 프런트가 바라볼 API 주소
 * ------------------------------------------------------------
 *  ★ 서버를 옮기거나 프런트만 따로 배포할 때는 이 한 줄만 고치면 된다.
 *
 *   같은 Node 서버가 프런트까지 서빙하는 경우 (기본값)
 *     ''            ← 같은 출처(same-origin). 그냥 비워 두면 된다.
 *
 *   프런트만 따로 띄우고 API 는 다른 서버인 경우
 *     'http://192.168.0.10:3000'
 *     'https://api.example.com'
 *
 *  ※ 화면 우측 상단 [API 주소] 버튼으로도 바꿀 수 있고,
 *     그 값은 브라우저 localStorage 에 저장되어 이 기본값보다 우선한다.
 * ============================================================ */
window.SOULRUSH_API_BASE = '';
